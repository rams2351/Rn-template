module.exports = {
  // The placeholder used in your app's code.
  // When the user runs "init NewApp", the CLI will replace this string
  // with "NewApp" in all your files.
 placeholderName: 'MyTemplate', 

  // The directory where the template code lives
  templateDir: './template'
};